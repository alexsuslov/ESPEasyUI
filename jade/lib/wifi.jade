h1
h4.cfg Networks List
ul.cfg.list-group

form.cfg(action='#')
  h4 Connect to:
  .form-group
    input.form-control(id="SSID", placeholder="SSID")
  .form-group
    input.form-control(id="KEY", placeholder="KEY")
  button.btn.btn-default(type="submit") Connect now!
